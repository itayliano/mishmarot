# Optional: enables the one-click "Add ALL shifts to Google Calendar" button.
# Without it, the app still works — each shift gets an "Add to Google Calendar" link
# and you can download an .ics file. See README for how to create a Client ID.
VITE_GOOGLE_CLIENT_ID=
